# Growful

Local Codex automation workspace.

## Packages

- `packages/reminder-mcp`: MCP server for scheduling reminder messages to a Codex thread.

## Commands

Run workspace commands from the repository root:

```sh
npm run build
npm test
npm run typecheck
```

Run the reminder MCP server during development:

```sh
npm run dev
```
